<Window x:Class="AXA.FOT.Application.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="350" Width="525">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Height="60" Width="60" Content="Offre" Command="{Binding cmdExecuteOffer}"/>
            <Button Height="60" Width="60" Content="Poseidon" Command="{Binding cmdExecutePoseidant}"/>
        </StackPanel>
        
        <ContentControl prism:RegionManager.RegionName="ContentRegion" />
    </StackPanel>
</Window>
